import React, { useState } from 'react';
import { render } from 'react-dom';

export function simp(e) {
    e.preventDefault();
    alert('1');
}
export function compo(e) {
    e.preventDefault();
    alert('2');
}
